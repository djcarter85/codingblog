---
title: "Fun ProcGen 1: Setup"
summary: Getting started with randomness.
---

_This post is part of a [series](/2024/12/05/FPG-0/) combining procedural
generation and functional programming._

follow along on GH. each post will have a branch

created a core library and a winforms project to view some of the results

To start with, we're going to need a source of randomness. For this, I'm going
to use [RandN](https://randn.dev/), a NuGet package providing alternative random
number generation to the built-in System.Random. The main thing I like about
RandN is how it defines an interface `IDistribution<T>`, which represents a
source of random `T`s (for some type `T`, e.g. `int`) corresponding to some
probability distribution.

```cs
public interface IDistribution<T>
{
    T Sample<TRng>(TRng rng);
}
```

That is, a distribution is a thing you can sample a value from, given a source
of randomness (the `rng` in the definition).

example distributions with pictres

something about eric lippert
<https://ericlippert.com/2019/01/31/fixing-random-part-1/>
