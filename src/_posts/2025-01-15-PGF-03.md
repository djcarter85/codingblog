---
title: "ProcGen Fun 3: Simple Mazes"
summary: TODO
---

_This post is part of a [series about procedural generation and functional
programming](/2024/12/25/PGF-00/). The source code for **this** post can be
found [here](https://github.com/djcarter85/ProcGenFun/tree/post-02)._

So far in this series we have given ourseleves the ability to generate random
values corresponding to some distribution. In this post we're going to actually
start generating something pretty, namely some mazes!

something about the book

## Representing the maze in code

To start with, we need a grid to work on. A grid is made up of cells, which we
can partition into rows and columns.

![Maze with all walls](/assets/images/2025-01-15-PGF-03/maze-all-walls.svg)

We can refer to columns using an X coordinate and rows using a Y coordinate
(0-indexed, of course!). For example, the highlighted cell in the below image is
the cell with X=4 and Y=6.

![Maze grid](/assets/images/2025-01-15-PGF-03/grid.svg)

We can represent this in code with `Cell` and `Grid` types.

```cs
public record Cell(int X, int Y);

public class Grid
{
    public Grid(int width, int height)
    {
        this.Width = width;
        this.Height = height;
        this.Cells =
            (from x in Enumerable.Range(0, this.Width)
             from y in Enumerable.Range(0, this.Height)
             select new Cell(x, y))
             .ToList();
    }

    public int Width { get; }

    public int Height { get; }

    public IEnumerable<Cell> Cells { get; }
}
```

We also need to keep track of which walls are present between cells. I've chosen
to represent this using the four cardinal directions on a map, but another
approach could be to store a wall as the pair of cells on either side of the
wall.

```cs
public enum Direction { North, East, South, West }
```

I then have a `Maze` class which keeps track of which walls are actually present
in any given maze.

```cs
public class Maze
{
    private readonly ImmutableDictionary<Cell, ImmutableList<Direction>> cellWalls;

    private Maze(Grid grid, ImmutableDictionary<Cell, ImmutableList<Direction>> cellWalls)
    {
        this.Grid = grid;
        this.cellWalls = cellWalls;
    }

    public Grid Grid { get; }

    public static Maze WithAllWalls(Grid grid)
    {
        var cellWalls = ImmutableDictionary<Cell, ImmutableList<Direction>>.Empty;

        foreach (var cell in grid.Cells)
        {
            cellWalls = cellWalls.Add(
                cell,
                [Direction.North, Direction.East, Direction.South, Direction.West]);
        }

        return new Maze(grid, cellWalls);
    }

    public bool WallExists(Cell cell, Direction direction) =>
        this.cellWalls[cell].Contains(direction);
}
```

something about immutable

## The binary tree algorithm

Enough chat; you came here for mazes, so let's make one!

The _binary tree algorithm_ is probably the simplest maze generation algorithm.
It works as follows:

- For each cell in the grid, choose randomly between North and East, and remove
  the wall in that direction.
- Along the North edge, you can't remove the North wall because it's the edge of
  the grid, so remove the East wall.
- Similarly, along the East edge, remove the North wall.
- For the North-East cell, don't remove any walls.

TODO make the description more fun!

TODO code, and explain the distribution composition stuff

And here's what it looks like!

![Maze generated using the binary tree
algorithm](/assets/images/2025-01-15-PGF-03/binary-tree.svg)

Notice how these mazes always have unbroken corridors along two of the sides.
This is an unfortunate bias of this algorithm; later in the series we'll
generate mazes with less obvious biases. One advantage of this algorithm is that
it's very fast because you only need to visit each cell once.

TODO show many outputs. maybe something about how immutability allows us to do that?

